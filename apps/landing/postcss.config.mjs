// Tailwind CSS v4 ships its own PostCSS plugin. No tailwind.config.js needed —
// configuration lives in CSS via the @theme directive (see app/globals.css).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
