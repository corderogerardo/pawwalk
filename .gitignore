# ---------- macOS ----------
.DS_Store

# ---------- Xcode / Swift (apps/ios) ----------
# *.xcodeproj/ is generated by XcodeGen — do not commit.
# (gitignore has no inline-comment syntax — a trailing "# ..." is part of the
# pattern itself, so it must live on its own line or the rule silently no-ops.)
*.xcodeproj/
!project.yml
xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
build/
.swiftpm/
*.swiftpm/.build/

# ---------- Android / Kotlin (apps/android) ----------
.gradle/
**/build/
local.properties
*.iml
.idea/
captures/
*.apk
*.aab
*.keystore
!debug.keystore

# ---------- Python / backend (apps/backend) ----------
__pycache__/
*.py[cod]
.venv/
.uv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.env
pawwalk.db

# ---------- Node / Next.js (apps/landing) ----------
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
npm-debug.log*

# ---------- Editors / misc ----------
.vscode/
!.vscode/extensions.json
